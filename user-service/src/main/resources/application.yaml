

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka-server
    register-with-eureka: true
    fetch-registry: true
server:
  port: 8081
spring:
  application:
    name: user-service