spring:
  application:
    name: task-service
server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka
    register-with-eureka: true
    fetch-registry: true